import CardContent from './CardContent';
import './UserCard.css';

function UserCard() {
  return (
    <div className="flippable-card-container">
      <CardContent />
    </div>
  );
}

export default UserCard;
